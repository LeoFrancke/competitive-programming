#include <bits/stdc++.h>

// using namespace std;
// avoiding the above code for now, just to get a feel of the language

int main() {
    // first day trying the language; please don't laugh at my code
    std::vector<set> username;
    std::cin >> username;

    if username.size() % 2 == 0 {
        std::cout << "CHAT WITH HER!";
    } else {
        std::cout << "IGNORE HIM!";
    }

}

