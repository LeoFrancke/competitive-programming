// standard library for input/output
#include <stdio.h>

// no arguments, so use void.
int main(void) {
    // always finish with a newline when using zsh, otherwise it displays % at the end
    printf("Hello C world!\n");
    return 0;
}

